<Window x:Class="PasswordManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password Manager"
        Width="1200"
        Height="700"
        ResizeMode="NoResize" Background="#FF282828" Icon="/VaultIcon.png" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border Background="{TemplateBinding Background}" CornerRadius="5"/>

                            <Border x:Name="InteractionOverlay" Background="White" Opacity="0" CornerRadius="6"/>

                            <ContentPresenter HorizontalAlignment="Center" 
                                          VerticalAlignment="Center" 
                                          Margin="{TemplateBinding Padding}"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="InteractionOverlay" Property="Opacity" Value="0.15"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="InteractionOverlay" Property="Background" Value="Black"/>
                                <Setter TargetName="InteractionOverlay" Property="Opacity" Value="0.2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#FF1F1F1F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="#FF1F1F1F"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#FF404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <!-- Bal oldali panel -->
        <Border Grid.Column="0"
                Background="#FF1F1F1F"
                BorderBrush="#FF282828"
                BorderThickness="0,0,1,0">

            <StackPanel Margin="10">
                
                <TextBlock Text="Password Manager" 
                           Foreground="White" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Margin="0,10,0,30" 
                           HorizontalAlignment="Center"/>

                <Button Content="Mentés"
                        Style="{StaticResource ModernButtonStyle}"
                        Height="40"
                        Margin="0,0,0,10"
                        Background="#FF5C933A" 
                        Click="Save_Click" />

                <Button Content="Kiválasztott törlése"
                        Style="{StaticResource ModernButtonStyle}"
                        Height="40"
                        Margin="0,0,0,10"
                        Background="#FFF33D21"
                        Foreground="White"
                        Click="Delete_Click"/>

            </StackPanel>

        </Border>

        <!-- Jobb oldal -->
        <Grid Grid.Column="1" Margin="10">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Szolgáltatás:"
                       Foreground="White"
                       Margin="0,0,0,5"/>

            <TextBox x:Name="ServiceTextBox"
                     Grid.Row="1"
                     Height="30"
                     Margin="0,0,0,10"/>

            <TextBlock Text="Jelszó:"
                       Grid.Row="2"
                       Foreground="White"
                       Margin="0,10,0,5"/>

            <PasswordBox x:Name="PasswordBox"
                         Grid.Row="3"
                         Height="30"
                         Margin="0,0,0,10"/>

            <TextBlock Text="Bejegyzések"
                       Grid.Row="4"
                       Foreground="White"
                       FontSize="18"
                       FontWeight="Bold"
                       Margin="0,10,0,10"/>

            <!-- Card list -->
            <ListBox x:Name="PasswordsList" 
                     Grid.Row="5"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     Background="Transparent"
                     BorderThickness="0">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#FF1F1F1F"
                                CornerRadius="8"
                                Padding="10"
                                Margin="0,0,0,10"
                                HorizontalAlignment="Stretch">

                            <Grid HorizontalAlignment="Stretch">

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Value}"
                                           Grid.Column="0"
                                           Foreground="White"
                                           FontSize="16"
                                           VerticalAlignment="Center"/>

                                <TextBlock x:Name="VisiblePasswordText" 
                                           Grid.Column="1"
                                           Foreground="#FF5C933A"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Margin="10,0,15,0"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>

                                <Button x:Name="ToggleBtn"
                                        Style="{StaticResource ModernButtonStyle}"
                                        Content="👁️ Mutat"
                                        Grid.Column="2"
                                        Padding="10,0"
                                        Margin="0,0,10,0"
                                        Height="35"
                                        Background="#FF3481BE"
                                        Foreground="White"
                                        Tag="{Binding}"
                                        Click="ToggleVisibility_Click"/>

                                <Button Content="📋 Másol"
                                        Style="{StaticResource ModernButtonStyle}"
                                        Grid.Column="3"
                                        Padding="10,0"
                                        Height="35"
                                        Background="#FF6DA446"
                                        Foreground="White"
                                        Tag="{Binding}"
                                        Click="CopyToClipboard_Click"/>

                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>

            </ListBox>

        </Grid>

    </Grid>

</Window>